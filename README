Parameters to edit on gameOfLife.f:

iterations: iter
 grid size: xwidth,ywidth

Parameters to edit on gnuplot.plot:

 grid size: xrange,yrange
 axis tics: x2tics,ytics
Compilation and running:
 run the executable runnit

NOTE: If runnit is not an executable:
      run the command: chmod +x runnit
